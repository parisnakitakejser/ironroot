apiVersion: v1
kind: Service
metadata:
  name: {{ include "ironroot.fullname" . }}
  labels:
    {{- include "ironroot.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: https
      port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
  selector:
    {{- include "ironroot.selectorLabels" . | nindent 4 }}
