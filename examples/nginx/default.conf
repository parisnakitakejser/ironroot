server {
    listen 443 ssl;
    server_name demo.home.arpa demo.local test.ironroot.local;

    ssl_certificate /certs/tls.crt;
    ssl_certificate_key /certs/tls.key;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}
