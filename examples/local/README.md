# Local IronRoot Demo

This directory is for local quick-start output. The guide writes:

- `pki/root/` for Root CA material.
- `pki/intermediate/` for Intermediate CA material.
- `data/` for the SQLite database.
- `certs/` for website TLS certificates.

Do not reuse local demo keys in production.
